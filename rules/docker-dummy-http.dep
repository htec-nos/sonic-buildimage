DPATH       := $($(DOCKER_DUMMY_HTTP)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-dummy-http.mk rules/docker-dummy-http.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_DUMMY_HTTP)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_DUMMY_HTTP)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_DUMMY_HTTP)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_DUMMY_HTTP),$(DOCKER_DUMMY_HTTP_DBG)))