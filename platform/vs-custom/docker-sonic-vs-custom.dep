#DPKG FRK
DPATH       := $($(DOCKER_SONIC_VS_CUSTOM)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) platform/vs-custom/docker-sonic-vs-custom.mk platform/vs-custom/docker-sonic-vs-custom.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SONIC_VS_CUSTOM)_CACHE_MODE  := GIT_CONTENT_SHA 
$(DOCKER_SONIC_VS_CUSTOM)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SONIC_VS_CUSTOM)_DEP_FILES   := $(DEP_FILES)

