#!/usr/bin/make -f

# See debhelper(7) (uncomment to enable).
# Output every command that modifies files on the build system.
include /usr/share/dpkg/pkg-info.mk

export DH_VERBOSE=1

PYTHON        ?= python3
PACKAGE_PRE_NAME := hello-sonic-pkg
SRC_DIR:= $(shell pwd)

%:
	dh $@ --with python3 --buildsystem=pybuild